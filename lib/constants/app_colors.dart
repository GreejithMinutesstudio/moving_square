import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Colors.amber;
  static const bgColor = Color(0xFFFFF8E1);
  static const buttonTextColor = Colors.white;
  static const textBlackColor = Colors.black;
}
